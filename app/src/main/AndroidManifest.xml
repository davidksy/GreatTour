<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a60047506.greattour">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DonActivity">
        <!-- <activity android:name=".MainActivity"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity android:name=".DonActivity" />-->
        <activity android:name=".PlusfriendActivity" />
        <activity android:name=".SharescheduleActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".EmailLoginActivity" />
        <activity android:name=".LoginidpwActivity" />
       <!-- <activity android:name=".CalendarActivity"></activity>-->
    </application>

</manifest>